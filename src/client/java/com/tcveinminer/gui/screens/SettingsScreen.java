package com.tcveinminer.gui.screens;

import net.minecraft.client.gui.screen.Screen;
import net.minecraft.text.Text;

/**
 * SettingsScreen — đã được hợp nhất vào MainMenuScreen (tab CẤU HÌNH PHỤ).
 * Giữ lại class này để tương thích với code cũ (RadialMenuScreen → Settings).
 */
public class SettingsScreen extends MainMenuScreen {

    public SettingsScreen(Screen parent) {
        super(parent);
    }
}
